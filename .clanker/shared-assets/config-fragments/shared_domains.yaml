sets:
  standard_repo_resolver: { id: "repo_content", type: "repo_content", includes: [".clanker"], excludes: [".git"], sorting: "normal" }

domains:
  - name: "test"
    renders:
      - name: 'test'
        resolvers:
          - { set: "standard_repo_resolver" }