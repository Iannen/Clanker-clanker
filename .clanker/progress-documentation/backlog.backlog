I. Ideas, complaints and non-critical bugs:

- arrow keys interpreted as esc
    - this has grown on me though - keep? but formalize if so
- the rendered UI does not properly display empty buttons for 'qwerty' and 'asdfgh' rows
    - this is due to the ui template being constrained to 4 buttons for the two keyrows.
    - ez fix is to adjust template.
    - rabbithole solution is to have app write template to rowspec

II. The pile
   
7. Landingzone for Validation / complaint ideas
    - manifest resolver must check if fileset is declared in pud_cfg with fallback to shared_cfg
        - so shared_cfg can declare defaults for it, which the pud_cfg can override
        - if not present, then complain
    - MD asset collision, fallback and absense
        - Files of Filelist, subject to the md resolver, referenced by name in Filelists, must follow these rules
            - there can be no pud internal or shared internal naming collisions
                - <pud>/onepath/file.txt and <pud>/another/file.txt is a violation
                    - because name is the same, it is pud internal collision. the same rule applies to shared internals
                - <pud>/onepath/file.txt and <shared>/another/file.txt is OK
                    - this is fine. the system uses fallback from pud to shared
        - so we need to validate that the files mentioned
            - exist in one or both repos
            - but are unique internally in any repo they exist
        - then manifest resolver can do less work

1. Add next numeral line to history output instructions
    [ ] 'a_lib/templates/documentation/backlog.template': Update history output instruction format
        - append a trailing 'Next item numeral: <FOLLOWING_ROMAN_NUMERAL>' marker to history output instructions
        - prevent Roman numeral sequence miscalculations when feeding truncated history files back to LLMs

Backlog tail, Selected item/items:

1. Contract verification and critical complaints support during bootstrap
    [ ] 'content/core/engine_deps/ingestion.py':
        - add BootstrapOutcome class with outcomes (NO_COMPLAINTS, NON_CRITICAL_ONLY, CRITICAL_BLOCKERS, CLANKERIZE_READY)
        - expand Report abstract interface with add_critical_complaint, get_critical_complaints, and get_outcome methods
    [ ] 'content/modules/asset_ingestion/commons/error_collector.py':
        - update ErrorCollector to store critical complaints alongside non-critical complaints
        - implement get_outcome logic evaluating presence of complaints and complete contract absence for CLANKERIZE_READY
    [ ] 'content/modules/asset_ingestion/ingestion_service.py':
        - update get_runtime_config signature to return 4-tuple (report, button_map, ui_render, base_resolvers)
        - remove NoConfig / ConfigAssembly exception raises from get_runtime_config and _get_validated_cfg_fragment
        - verify CfgFragments presence and YAML parsing; record critical complaints on failure and fallback to empty dicts
        - verify RepoContract members presence on disk; record critical complaints for missing elements
        - attempt execution of extractors, assemblers, and validators to amass non-critical complaints
    [ ] 'content/core/core/presentation.py':
        - add presentation formatter/helper to convert Report outcome and complaints into ActionResult or prompt string
        - support formatting CRITICAL_BLOCKERS into dual sections displaying both critical and non-critical complaints
    [ ] 'content/core/engine.py':
        - remove _bootstrap method and fold bootstrap state handling directly into run()
        - delegate Report handling to presentation formatter to produce initial ActionResult or prompt
        - execute control flow based on BootstrapOutcome state:
            * NO_COMPLAINTS: assign services and enter main TUI loop
            * NON_CRITICAL_ONLY: prompt user for confirmation before entering TUI loop
            * CRITICAL_BLOCKERS: return formatted failure ActionResult to exit application
            * CLANKERIZE_READY: prompt user for initialization confirmation, call initialize_workspace, and re-run ingestion

Backlog tail end
