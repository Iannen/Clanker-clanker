I. Ideas, complaints and non-critical bugs:

- arrow keys interpreted as esc
    - this has grown on me though - keep? but formalize if so
- the rendered UI does not properly display empty buttons for 'qwerty' and 'asdfgh' rows
    - this is due to the ui template being constrained to 4 buttons for the two keyrows.
    - ez fix is to adjust template.
    - rabbithole solution is to have app write template to rowspec

2. clankerization of a project can represent the much talked about repo contract by the content it creates
    - it should check 'does any of the content exist'?
        - if so dont do anything, complain to user
    - only if coast clear should it create the stuff in repo root
        - '.clanker' dir and its internal stuff
        - 'README.md' -> #empty
        - 'content' -> no members

3. i should be able to add more than just filesets as named bottom level sets.
    - we have 3 resolvers that deal with files
        - md resolver has fallback to shared logic, uses Filelist which contain File 
            - this should become a bottom level thing
        - repo and manifest resolvers both use Filesets
            - this is already a bottom level thing. do Fileset class need an attr to say source? 
                - It can default to pud
                - and optionally by some flag var work off of shared instead
            - then improves semantics of yaml?

4. Validation / complaints
    - manifest resolver must check if fileset is declared in pud_cfg with fallback to shared_cfg
        - so shared_cfg can declare defaults for it, which the pud_cfg can override
        - if not present, then complain

5. Make use of latest architectural work in ingestion module
    [ ] simplify EC and 'Report' for now, lets just have str msgs for ez
        - so prune the typed thing, and just use the 1 type of complaint registration for all cases
    - Here we add complaintreasons we can think of, as they pop into our head. they should be represented by a method with the complaintproducer

6. UI / Feedback imp [DX/UX]: 
    - recent architectural work should make this easier
    - the UI can produce a better header message, and also perhaps a filetree on the right hand side, where there is ample space

7. Non IDE / linter import strats: BUFF importance of this, as LLMs typically run lower than 3.14 version of python, and cannot compensate.
    - goals:
        - We dont have huge import blocks in all the numerous files, eating up precious llm context
        - all types used in a file are always imported (explicitly or via kleene)
            - this goes for typehints too
        - the inverse. no dangling imports allowed
            - but what abt * imports?
    - something about python v 3.14 vs older versions - a clue!
    - i added instruction to prune in the do-mode.md file, so it gets done bit by bit at least

8. Mull 'ExceptionPolicy'
    - its purpose is essentially to classify ex that core does not deal with
    - and to format the msgs that get printed in main
    - 'protection' must be done up in main
    - while formatting via method is in appengine (it receives the ex)
        - perhaps it all goes to main? 

9. Follow up prompts
    - to pass the next step of workflow
        - plan into do into backlog management
    - reduce token waste
    - quicker iterations

10. Clean up ports and adapters stuff: 
    Phase 1: adapter refactoring & naming
    [x] 'content/ports_adapters/ports.py': Rename ports and align exception contracts
        - Rename FileBridgePort to DiskPort, IOBridgePort to TerminalPort, and ConfigIngestorPort to ConfigParserPort
        - Keep translated infrastructure exceptions (NoSuchFile, FileAccessError) adjacent to the ports
    [x] 'content/ports_adapters/disk_adapter.py': Rename and refine LinuxDiskAdapter
        - Rename FileBridge class to LinuxDiskAdapter implementing DiskPort
        - Ensure proper exception translation boundaries
    [x] 'content/ports_adapters/terminal_adapter.py': Populate LinuxTerminalAdapter
        - Move IOBridge out of legacy adapters.py and rename to LinuxTerminalAdapter implementing TerminalPort
    [x] 'content/ports_adapters/yaml_parser.py': Populate RuamelYamlParserAdapter
        - Move ConfigIngestor out of legacy adapters.py and rename to RuamelYamlParserAdapter implementing ConfigParserPort
    [x] 'content/ports_adapters/adapters.py': Clean up and remove legacy file
        - Remove old definitions now that each adapter resides in its own isolated file (user will delete on ack)
    [x] 'content/clanker.py': Update entry point imports and instantiation wiring
        - Update initialization to use LinuxDiskAdapter, LinuxTerminalAdapter, and RuamelYamlParserAdapter

    Phase 2: core facing
    [x] 'ports.py'
        - 'getFileContent' is not used and can be pruned from port and adapter
    [ ] LinuxDiskAdapter cannot pass str arg to any notice, it violates ex policy.
    [ ] core must deal with LDA's raising of 'FileAccessError'
    let us find out how the situation is at the call sites
    [ ] Let us then look to the adapters for easy wins with regards to code quality

II. Items to refine & QC:
III. Slated for implementation:
1. Implement initial headless test system and quality gate pipeline
    Phase 1: Setup / initial work
    [x] 'content/ports_adapters/terminal/scripted_terminal_adapter.py': Implement ScriptedTeminalAdapter adhering to TerminalPort
        - Record UI frames, inputs consumed, and clipboards
        - Flush execution metrics to JSON report file on output write
    [x] 'content/clanker.py': Add command-line flag support for headless test mode execution
        - Support --test, --input-script, and --report-path parameters
        - Swap LinuxTerminalAdapter for ScriptedTeminalAdapter when running in test mode
    [x] 'content/test/ship_gate.py': Implement OOP two-phase quality gate architecture
        - BaseFixtureTest manages test orchestration, sandboxing, and execution
        - GateInspector evaluates subclass JSON reports and outputs formatted results
    [x] 'content/test/test_repos/empty_repo/': Establish persistent source fixture directory templates
        - Serve as immutable template for sandbox isolation
    [x] 'content/test/assert_classes.py': Implement declarative test fixture class EmptyRepoTests
        - Expose assert_escape_exits and assert_ctrl_c_exits return specs
        - Define input sequences and expected exit codes/messages
    [x] 'content/test/ship_gate.py': Automate test lifecycle management and sandbox isolation
        - Recreate active sandboxes from source templates per test class run
        - Execute headless clanker.py subprocesses and output structured JSON reports to content/test/reports/
    Phase 2: machinery improvements
    [x] 'content/test/ship_gate.py': Shift teardown responsibility to global test suite initialization
        - Remove per-class teardown logic inside BaseFixtureTest setup lifecycle
        - Execute a single wipe (rm -rf) of content/test/sandboxes/ and content/test/reports/ at the start of ship_gate run
        - BaseFixtureTest creates sandbox once per class, runs asserts (aggregating outcomes), and writes final report
    [x] 'content/ports_adapters/terminal/scripted_terminal_adapter.py': Implement character stream flattening in ScriptedTerminalAdapter
        - Accept strings or lists of mixed strings/control characters in input_sequence
        - Automatically expand string tokens into individual character elements while preserving special control sequences (e.g., "yes" expands to "y", "e", "s")
    Phase 3: next assert & prereqs of it
    [ ] 'content/test/ship_gate.py': Support filesystem domain inspection in BaseFixtureTest execution
        - Support 'fs_paths_exist' and 'fs_paths_absent' keys inside expected spec dictionary
        - Verify path existence relative to active_sandbox directory post-execution
    [ ] 'content/test/assert_classes.py': Add assert_clankerize_repo_contract assertion to EmptyRepoTests
        - Input sequence to accept workspace initialization
        - Assert process exit code/message and presence of .clanker directory/files inside sandbox

IV. Recently implemented:
V. Critical bugs
on test repo, manifest domain for 'a' only (keys 1 -> a):
'''
[FATAL] FileAccessError: File access error for presentation/image_lib/clanker_boot.png: 'utf-8' codec can't decode byte 0x89 in position 0: invalid start byte

--- Underlying Stack Trace ---
Traceback (most recent call last):
  File "/home/user/Projects/Clanker-clanker/content/ports_adapters/adapters.py", line 72, in _pud_file_as_string
    return (self.pud_path / rel_path).read_text(encoding="utf-8")
           ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^
  File "/usr/lib/python3.14/pathlib/__init__.py", line 788, in read_text
    return f.read()
           ~~~~~~^^
  File "<frozen codecs>", line 325, in decode
UnicodeDecodeError: 'utf-8' codec can't decode byte 0x89 in position 0: invalid start byte

~/Projects/Clanker-clanker main*
'''
on test repo, manifest domain for 's' only (keys 1 -> s):
works fine, but dont use a manifest resolver..


