I. Ideas, complaints and non-critical bugs:
- configs should be validated to disallow non-existent filesets (now just fail silently)
- filesets: new concept, 'omits' added as a manifest of files not supplied
- Visual upgrade to UI template
- the user should not have to start & stop the app for config updates to become online
- can we do without any external deps like ruamel & pydantic, lessening the install burden?
- arrow keys interpreted as esc

II. Items to refine & QC:

1.  Leverage ports & adapters split -> offload non-business logic to adapters, leave behind clean code
    The classes that depend directly upon adapters are candidates for review:
        - SessionService
        - AssemblyService
        - IOService   
    Methodology is review of service class methods
        - the usage of 'self.files.write_default_documents' in SessionService.initialize_workspace is thought to be a good example of desired outcomes.
        - do other methods carry out stuff that the adapter could take care of for them?
    [ ] 'SessionService._resolve_sets':
            - copy.deepcopy() usages
            - perhaps offload to new transformers protocol
    [ ] ' AssemblyService.hydrate':
            - offload to a new transformers protocol?
            - does 'AssemblyService.hydrate' become passthrough - or do I allow caller protocol access?
    [ ] various usages of 'Path'
            - is this a simple 'move path stuff inside existing bridge' job?

2a. Filebridge & adapter tokenized path normalization
    - getAssetMap & getFileContent: serve MULTI_DOC of AssemblyService._resolve only (wait with this)
        - So perhaps the bridge can deliver the finished product in one call?
        - That would obscure the truncation, which is not desireable
        - anyway, should the new method be tokenized? i think perhaps not -> it could work like 'first pud, then shared'
        - so we get some bridge methods which are tokenized, and others that work off of a fallback from pud to shared

III. Slated for implementation:
2b. Filebridge & adapter tokenized path normalization
    [x] 'clanker.py' & 'adapters.py': combine 'read_clanker_asset' & 'read_pud_asset' into 'read_asset'
        - delete 'read_clanker_asset' & 'read_pud_asset' from 'FileBridgePort' and 'FileBridge'
        - add new member 'read_asset(self, tokenized_path: str | Path) -> str:' to both
            - impl, uses 'startswith' to find correct base, similar to existing methods
            - no docstring on port please
    [x] 'clanker.py': callsite alignments:
            - Update AS.get_template to prepend 'BasePathTokens.SHARED'.
            - Update AS._resolve calls to prepend 'BasePathTokens.PUD' or 'BasePathTokens.SHARED'
            - Refactor AS._build_manifest_block to eliminate the token if/else branching.
    [ ] 'clanker.py': Normalize path constants in Section 3
        - Ensure all path strings in Section 3 classes start with '/'
        - Remove all BasePathTokens references and embedded token strings from Section 3 path members
    [ ] 'clanker.py': Update callsites for path string assembly
        - Update all callsites to combine base path tokens and Section 3 path constants using 'BasePathTokens.X + Y.Z'
        - Eliminate inline path strings and inline token concatenation logic across all service classes

IV. Recently implemented:

V. Critical bugs
