I. Ideas, complaints and non-critical bugs:

- arrow keys interpreted as esc
    - this has grown on me though - keep? but formalize if so
- the rendered UI does not properly display empty buttons for 'qwerty' and 'asdfgh' rows
    - this is due to the ui template being constrained to 4 buttons for the two keyrows.
    - ez fix is to adjust template.
    - rabbithole solution is to have app write template to rowspec

1. UI / Feedback imp [DX/UX]: 
    - make prompt compilation msg say which domain, prompt was used in addition to just line/char
    - turns into a pipeline refactor. oh well
    - we have the righthand side of the screen to put info on. will be baller, only green (black) grass

2. i should be able to add more than just filesets as named bottom level sets.
    - MD resolver prime target, I can put it in shared cfg -> reuse it in domains of pud cfgs
    - lets not rush it tho

3. revise new dir structure, so we dont have simple 'code.py' files, they should ref their system

4. Non IDE / linter import strats: BUFF importance of this, as LLMs typically run lower than 3.14 version of python, and cannot compensate.
    - goal, make it so that 
        - all types used in a file are always imported (explicitly or via kleene)
            - this goes for typehints too
        - the inverse. no dangling imports allowed
            - but what abt kleene imports?
    - something about python v 3.14 vs older versions?

5. Simple 'end to end' test system, for improved dev confidence when iterating
   Modeled on my manual 'test flow':
    - For a clankerized repo
        - the app should start, display UI
        - all domain buttons should
            - if inhabitet, display proper msg
            - if unhabited, display proper msg
        - then, for each domain button, every prompt button should
            - if prompt button inhabitet, produce a prompt of expected size
            - if no inhabitant, display appropriate msg
        - for any and all buttons outside the keys specified by system_cfg
            - be silently ignored, and not cause crash as they do now
            - exception for things like esc, ctrl+c, ctrl+d and other 'system keys'
    - When clankerizing a repo:
        - it should produce a repo on the expected format, containing all members of a yet to be declared 'repo contract'
        - then it should pass the 'clankerized repo' test
        - perhaps also a 'unruly repo' scenario, where the repo already contains things contract mandates
            - > than complain to user, do no harm
        - so clankerization must be safe. Perhaps the confirm msg can first check and see that it is so? else complain to user 

6. App failure modes razzia, not related to ingestion or later file not found business in Renderservice:
    - buttons not 'in scope' should produce a msg saying so (i get nonetype crash cuz no msg)
    - what else

7. Revise god file 'models.py' and members
    [ ] split it up into 
        - entities
        - exceptions
        - statics
        - presentation
    [ ] Button.get_repl_map -> migrate to Contentshaper
    [ ] ActionResult.get_msg -> migrate to Contentshaper

    - 4 laters: Perhaps the so called runtimeconfig can be split up on reception by engine, and passed into the module services that use them?
        - ui_render -> RenderService on self
        - Keyboard members -> into a service which deals with state & execution? KeyboardService returns oO?
            - absorbing some engine functionality too
        - RuntimeConfig becomes a 'product' returner to engine, unwrapped and distributed to other services?
            - 'base_resolvers' managed by RenderService on self?
    - benefits sought:
        - I dont have to pass the 'whole motherload' to llms, but I can tailor it more
            - reduce token use
            - reduce llm confusion
    - prettiness of organisation?
    - limits the fallout of kleene imports
        - which are beneficial for the token savings
    - but not huge feature boost. mainly shuffles things around

8. Ports & Adapters revision
    - do away with 'adopted notices' in ExceptionPolicy, which was a misstep
    - make adapters translate ex vocabulary into app vocabulary
    - keep bridgeleakage, for good error formatting screaming at dev what issue is at top of msg
    - so keep protect adapter functionality, but remove adoption in favour of translation
    - this is stylepoints chase mostly
        - but later good for when we have windows / mac adapters / no OSC52 alternative adapters
        - which entrypoint loads as appropriate for environment

9. Make use of latest architectural work in ingestion module
    - simplify EC and 'Report' for now, lets just have str msgs for ez

II. Items to refine & QC:

III. Slated for implementation:
10. Confine Path to adapter layer and implement exception translation
    [x] Extract static path specifiers and constants from 'models.py'
        [x] Moved IOControl to tui/tui_service.py, updated imports
        [x] Moved CfgFragments, Layout, Config and BasePathTokens to 'content/app/constants.py'
            - They all deal with paths, so it makes sense to keep them in a single location
        [x] Moved SystemKeys containing delim to renderservice.
            - its then passed on down to contentshaper, to avoid circular import. ugh            
            - button imports it in contentshaper bount get repl method
        - this empties model.py of such classes, and centralizes path constants in one place for ez of analysis
    [x] Removed from __future__ import annotations from codebase

    [ ] 'content/ports_adapters/ports.py': Remove Path from FileBridgePort signatures
        - change 'read_asset' parameter tokenized_path to 'str'
        - change 'get_files' rel_roots to 'list[str]' and return type to 'set[str]'
        - change 'getFileContent' parameter full_path to 'str'
    [ ] 'content/ports_adapters/adapters.py': Update FileBridge method signatures and return types
        - align 'read_asset', 'get_files', and 'getFileContent' parameter types to 'str'
        - ensure 'get_files' returns 'set[str]' using string representations of relative paths
        - retain 'Path' strictly inside 'FileBridge' method bodies for filesystem operations
    [ ] 'content/asset_ingestion/validators/assets.py': Update AssetValidator to operate on string path sets
        - change 'pud_pathlist' and 'shared_pathlist' annotations to 'set[str]'
        - replace 'Path' checks and 'Path.parents' operations with pure string path manipulations
        - remove 'pathlib.Path' import

    [ ] Translate adapter filesystem exceptions into domain exceptions (POSTPONED!)
        - wrap low-level 'FileBridge' operations to catch stdlib exceptions ('FileNotFoundError', 'PermissionError', etc.) and re-raise as domain exceptions
        - remove translated exceptions from 'ExceptionPolicy.ADOPTED_NOTICES'

IV. Recently implemented:

6. Fix AttributeError crash on out-of-scope keypress
    [x] 'app/engine.py': Guard 'self.msg' updates during key handling
        - update loop in 'run' to store 'handle_key' result in local variable
        - only reassign 'self.msg' if the result is not None

V. Critical bugs
 