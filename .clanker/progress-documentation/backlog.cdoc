I. Ideas, complaints and non-critical bugs:
- arrow keys interpreted as esc
- the rendered UI does not properly display empty buttons for 'qwerty' and 'asdfgh' rows
    - this is due to the ui template being constrained to 4 buttons for the two keyrows.
    - ez fix is to adjust template.
    - rabbithole solution is to have app write template to rowspec

II. Items to refine & QC:

1. qol imp: 
    - make prompt compilation msg say which domain, prompt was used in addition to just line/char
    - turns into a pipeline refactor. oh well

2. MUST find a way to make the system bitch out whenever a type hint is not in scope
    - this to enforce that protocol are not just some paper layer
    - dev-time is IDE rabbithole
    - perhaps some test for this? this is what linters and shit do.
        - cons: heavy NIH
        - pros: project self-containment -> IDE independence
    - also import hygiene!

3. i should be able to add more than just filesets as named bottom level sets.
    - MD resolver prime target, I can put it in shared cfg -> reuse it in domains of pud cfgs

4. revise new dir structure, so we dont have simple 'code.py' files, they should ref their system

5. Improve config_assembly_system complaint production
    - consideration : BaseException inheritors bypass things? must be found out
    [ ] 'config_assembly_system/code.py'
        - add 'class DomainOverflow'
            - members to hold info for below
            - get_msg()->str:
                - DomainOverflow: <sys_cfg_name> declares <row_keys> for domains <domain_names> of <cfg_filename>, n overflowing
        - 'ErrorCollector'
            - add method to accept DomainOverflow  complaints or args required to fashion one., adding it to internal collection of DomainOverflow complaints
            - add method 'get_domain_overflow_report -> str | None'
                - if none, then no issue. if yes, then its part of a msg

    [ ] 'config_assembly_system/code.py': 
        - RuntimeConfigBuilder: 
            - when zipping buttons, if overflow, then complain to ErrorCollector, using above.
            - simply omit the overflow - the 'do the best we can but complain' approach (?)

    [ ] 'assembly_system_contract.py'
        - 'Report'
            - add get_domain_overflow_report -> str | None

    [ ] 'clanker.py': Refine complaint consumption for capacity and missing fileset cases
        - SessionService.get_runtime_config:
            - return report & rtc to appengine
        - AppEngine.bootstrap: determines if we can proceed based on the report
            - dof_report = report.get_domain_overflow_report
            - if dof is not None
                - craft a msg, embed in ex

6. Revert to ABC for the python interface construct of choice
    - consider this, for the benefit of runtime 'no sir'
    - and also forces import discipline 
    - cuz current Protocols are papertigers

III. Slated for implementation:

IV. Recently implemented:

V. Critical bugs
