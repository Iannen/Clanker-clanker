I. Ideas, complaints and non-critical bugs:
- configs should be validated to disallow non-existent filesets (now just fail silently)
- filesets: new concept, 'omits' added as a manifest of files not supplied
- Visual upgrade to UI template
- the user should not have to start & stop the app for config updates to become online
- can we do without any external deps like ruamel & pydantic, lessening the install burden?
- arrow keys interpreted as esc

II. Items to refine & QC:

1.  Data model revamp, unused alt-mode functionality removal. We split this up into several jobs:
    - pud cfg alignment debt:
        - rename 'sets' -> 'filesets'
        - update 'shared_domains.yaml' -> 'shared_cfg.yaml'
        - update 'kb_def.yaml' -> 'system_cfg.yaml'
    [ ] 'models.py': New set of domain model classes: 
'''
class RuntimeConfig: # purportedly fully aligned
    keyboard: Keyboard
    ui_render: Render
    base_resolvers: [Resolver]

class Keyboard: # purportedly fully aligned
    button_map: dict[str, Button] 
    selected_key: str | None
    def get_unique_buttons
    def handle_key 

class Button:
    type: str
    key: str
    inhabitant: Domain | Prompt | None = None
    action: Callable | None = None
    def get_repl_map

class Domain
    name: str
    resolvers: [Resolver]
    prompts: [Prompt]

class Prompt 
    name: str
    renders: [Render]

class Render -> 
    template: str = "prompt_template"
    resolvers: [Resolver]
    inherit_base: bool = True
    inherit_domain: bool = True

class Resolver:
    anchor: str
    def get_repl_map: dict[str, str]

class MultiDocResolver(Resolver):
    files: [File]

class File:
    name: str
    full_path_from_pud: bool = False
    truncation_spec: TruncationSpec

class TruncationSpec:
    # for more advanced truncation laters, for now just tailtrunc int
    tail_lines: int | None

class RepoContentResolver(Resolver):
    fileset: FileSet

class FileManifestResolver(Resolver):
    pud_fileset: Fileset
    shared_fileset: Fileset

class KBStateResolver(Resolver):

class FileSet -> to deal with filesets of repo and manifest resolver first class in domain 
    includes: [str]
    excludes: [str]
'''

III. Slated for implementation:

IV. Recently implemented:

1b. Half-align 'Button' model & remove legacy alt-mode / shift-action code ( this is converted to hist )
    [x] 'models.py': Update Button & Keyboard models
        - Rename Button.primary_letter -> key and Button.primary_action -> action
        - Remove Button.secondary_letter and Button.shift_action
        - Preserve inhabitant: Domain | Render | None type binding (defer Prompt class introduction)
        - Update Button.get_repl_map and Keyboard methods to use key and action
    [x] 'clanker.py': Clean up key handling, service normalization & engine bindings
        - Update IOService.get_key to normalize navigation key input to lower-case
        - Simplify Keyboard.handle_key to dispatch solely on key and action
        - Remove all shift_action bindings and NotImplementedError lambda stubs in GameEngine
        - Update AssemblyService._resolve (KB_INFO block) to use btn.key instead of btn.primary_letter
    [x] 'adapters.py': i reason that this requires no attention based on contents of 'clanker.py' and 'models.py'
        - it only returns chars. the service will normalize 
    [x] 'utilities.py': Refactor RuntimeConfigAssembler button map creation
        - Update _build_button_map to construct Button using single key sequence without secondary mapping
        - Update domain_keys lookup in assemble() to read single key sequence directly
    [x] 'system_cfg.yaml': Remove secondary row properties from kb_def schema
        - Remove all 'secondary' key mappings under domain_row, prompt_row, and action_row
        
V. Critical bugs
