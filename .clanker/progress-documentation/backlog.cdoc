I. Ideas, complaints and non-critical bugs:

- arrow keys interpreted as esc
    - this has grown on me though - keep? but formalize if so
- the rendered UI does not properly display empty buttons for 'qwerty' and 'asdfgh' rows
    - this is due to the ui template being constrained to 4 buttons for the two keyrows.
    - ez fix is to adjust template.
    - rabbithole solution is to have app write template to rowspec

5. Simple 'end to end' test system, for improved dev confidence when iterating
   Modeled on my manual 'test flow':
    - For a clankerized repo
        - the app should start, display UI
        - all domain buttons should
            - if inhabitet, display proper msg
            - if unhabited, display proper msg
        - then, for each domain button, every prompt button should
            - if prompt button inhabitet, produce a prompt of expected size
            - if no inhabitant, display appropriate msg
        - for any and all buttons outside the keys specified by system_cfg
            - be silently ignored, and not cause crash as they do now
            - exception for things like esc, ctrl+c, ctrl+d and other 'system keys'
    - When clankerizing a repo:
        - it should produce a repo on the expected format, containing all members of a yet to be declared 'repo contract'
        - then it should pass the 'clankerized repo' test
        - perhaps also a 'unruly repo' scenario, where the repo already contains things contract mandates
            - > than complain to user, do no harm
        - so clankerization must be safe. Perhaps the confirm msg can first check and see that it is so? else complain to user 

10. clankerization of a project can represent the much talked about repo contract by the content it creates
    - it should check 'does any of the content exist'?
        - if so dont do anything, complain to user
    - only if coast clear should it create the stuff in repo root
        - '.clanker' dir and its internal stuff
        - 'README.md' -> #empty
        - 'content' -> no members



2. i should be able to add more than just filesets as named bottom level sets.
    - we have 3 resolvers that deal with files
        - md resolver has fallback to shared logic, uses Filelist which contain File 
            - this should become a bottom level thing
        - repo and manifest resolvers both use Filesets
            - this is already a bottom level thing. do Fileset class need an attr to say source? 
                - It can default to pud
                - and optionally by some flag var work off of shared instead
            - then improves semantics of yaml?

8. Validation / complaints
    - manifest resolver must check if fileset is declared in pud_cfg with fallback to shared_cfg
        - so shared_cfg can declare defaults for it, which the pud_cfg can override
        - if not present, then complain

7. Make use of latest architectural work in ingestion module
    - simplify EC and 'Report' for now, lets just have str msgs for ez


1. UI / Feedback imp [DX/UX]: 
    - make prompt compilation msg say which domain, prompt was used in addition to just line/char
    - turns into a pipeline refactor. oh well
    - we have the righthand side of the screen to put info on. will be baller, only green (black) grass

4. Non IDE / linter import strats: BUFF importance of this, as LLMs typically run lower than 3.14 version of python, and cannot compensate.
    - goals:
        - We dont have huge import blocks in all the numerous files, eating up precious llm context
        - all types used in a file are always imported (explicitly or via kleene)
            - this goes for typehints too
        - the inverse. no dangling imports allowed
            - but what abt * imports?
    - something about python v 3.14 vs older versions - a clue!

II. Items to refine & QC:

7. Refactor keyboard management into dedicated KBService
    Phase 1: Dissolve RuntimeConfig container into explicit components
    [x] 'content/app/entities.py': Remove RuntimeConfig dataclass
        - Delete RuntimeConfig definition to eliminate monolithic config passing
    [x] 'content/app/deps/ingestion.py': Update IngestionService interface return type
        - Update get_runtime_config to return tuple[Report, Keyboard, Render, list[Resolver]]
    [x] 'content/asset_ingestion/ingestion_service.py': Unpack RtcAssembler components in return tuple
        - Return keyboard, ui_render, and base_resolvers directly instead of instantiating RuntimeConfig
    [x] 'content/app/deps/render.py': Update RenderService interface signature
        - Replace RuntimeConfig parameter in get_repl_map with explicit keyboard and base_resolvers dependencies
    [x] 'content/render_pipeline/render_service.py': Adapt render service callsites
        - Update get_repl_map signature and internal calls to accept keyboard and base_resolvers directly
    [x] 'content/app/engine.py': Store unpacked config components on self
        - Unpack tuple from session in _bootstrap and assign self.keyboard, self.ui_render, and self.base_resolvers
        - Update internal _render callsites to pass self.keyboard and self.base_resolvers
    [x] 'content/asset_ingestion/validators/assets.py': Update AssetValidator interface signature
        - Replace RuntimeConfig parameter in validate with explicit keyboard, ui_render, and base_resolvers dependencies

    Phase 2: Shift UI rendering ownership and lifecycle to RenderService
    [x] 'content/app/deps/render.py': Expand RenderService interface
        - Add set_ui_render(ui_render: Render) -> None
        - Add render_ui(keyboard: Keyboard, msg: ActionResult | None) -> str
    [x] 'content/render_pipeline/render_service.py': Implement render_ui
        - Store ui_render internally via set_ui_render during bootstrap
        - Implement render_ui(keyboard, msg): handle template loading, _res_ui replacement mapping, msg string extraction/injection, and hydration
    [x] 'content/app/engine.py': Delegate UI string generation to RenderService
        - Pass ui_render to self.renderer.set_ui_render during bootstrap
        - Simplify _display_ui to pass self.keyboard and self.msg directly into self.renderer.render_ui(self.keyboard, self.msg)

    Phase 3: Introduce KBService & streamline AppEngine (lookahead item)
    [x] Create KBServiceImpl class: just a dummy class that accepts button map on self
        - add set_button_map(self, button map):
            - store on self
    [x] Create KBService abc class as app deps member
    [x] clanker.py: add KBService to DI, set it on AppEngine
    [x] AppEngine
        - change init to accept KBService, store on self
        - on bootstrap, extract Keyboard.button_map, set on KeyboardService
        - retain all other workings as-is
    
    Phase 4: Engine Loop & KBService promotion
    [x] 'content/app/deps/keyboard.py': Define RenderContext and update KBService ABC
        - Create RenderContext dataclass holding (render: Render, keyboard: Keyboard, base_resolvers: list[Resolver])
        - Replace set_button_map with setup(keyboard: Keyboard, base_resolvers: list[Resolver]) -> None on KBService ABC
        - Update handle_key signature to return tuple[ActionResult | None, RenderContext | None]
    [x] 'content/keyboard/keyboard_service.py': Implement key handling & button state orchestration
        - Implement setup to store keyboard and base_resolvers, and wire domain button actions
        - Move _set_selected_num_btn logic into KBServiceImpl
        - Implement handle_key to handle domain button state updates, return (None, None) on unhandled keys, or return RenderContext for prompt actions
    [x] 'content/app/engine.py': Delegate key routing to KBService and compile prompt from RenderContext
        - Remove _wire_num_row and _set_selected_num_btn from AppEngine
        - Update _bootstrap to call kb_service.setup(keyboard, base_resolvers)
        - Refactor run loop to unpack (ActionResult, RenderContext) from KBService.handle_key
        - Extract render, keyboard, and base_resolvers from RenderContext to invoke RenderService methods directly and pass output to clipboard
III. Slated for implementation:    
IV. Recently implemented:
    Phase 5: AppEngine to Services delegation of menial tasks
    [x] 'content/app/deps/render.py' & 'content/render_pipeline/render_service.py': Facade prompt rendering in RenderService
        - Add `render_prompt(ctx: RenderContext) -> str` to RenderService ABC and implementation
        - Encapsulate `get_template`, `get_repl_map`, and `hydrate` sequence inside `render_prompt` without passing UI message context
        - No need to refactor RenderService beyond adding the new methods
            - simply use its existing methods inside the new method
    [x] 'content/app/deps/tui.py' & 'content/tui/tui_service.py': Delegate clipboard action result
        - Update `to_clipboard(content: str)` signature to return `ActionResult`
        - Move line and character count message formatting into `TUIServiceImpl.to_clipboard`
    [x] 'content/app/engine.py': Streamline engine run loop
        - Retain investigatory logic to determine if `self.msg` needs new assignment based on service returns
        - Remove `_display_ui` and inline `renderer.render_ui`, `io.display`, and `io.get_key` directly at the top of the run loop
        - Refactor prompt handling branch: call `renderer.render_prompt(render_ctx)` and assign `io.to_clipboard(...)` directly to `self.msg`

V. Critical bugs

well, not that critical for a WIP imo
on test repo, manifest domain for 'a' only (keys 1 -> a):
'''
[FATAL] FileAccessError: File access error for presentation/image_lib/clanker_boot.png: 'utf-8' codec can't decode byte 0x89 in position 0: invalid start byte

--- Underlying Stack Trace ---
Traceback (most recent call last):
  File "/home/user/Projects/Clanker-clanker/content/ports_adapters/adapters.py", line 72, in _pud_file_as_string
    return (self.pud_path / rel_path).read_text(encoding="utf-8")
           ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^
  File "/usr/lib/python3.14/pathlib/__init__.py", line 788, in read_text
    return f.read()
           ~~~~~~^^
  File "<frozen codecs>", line 325, in decode
UnicodeDecodeError: 'utf-8' codec can't decode byte 0x89 in position 0: invalid start byte

~/Projects/Clanker-clanker main*
'''
on test repo, manifest domain for 's' only (keys 1 -> s):
works fine, but dont use a manifest resolver..


