I. Ideas, complaints and non-critical bugs:

- arrow keys interpreted as esc
    - this has grown on me though - keep? but formalize if so
- the rendered UI does not properly display empty buttons for 'qwerty' and 'asdfgh' rows
    - this is due to the ui template being constrained to 4 buttons for the two keyrows.
    - ez fix is to adjust template.
    - rabbithole solution is to have app write template to rowspec

- bootstrap / repo init
    - if 'content' dir is empty, then it doesnt get recognized (if a fileset wants it)
    - bootstrap validation does not understand the '.' to mean 'the whole repo', but think there is a dir named '.' that must exist
        '[shared] asset include '.' (<domain=debloat, prompt=debloat, resolver=RepoContentResolver, include=.>) was not found in pud'
    - the repo init must also create the 'content' dir, to fulfill contract


A. Deprioritized / Housekeeping

2. Non IDE / linter import strats: 
    note: not so important, just nice for bragpacks
    - goals:
        - We dont have huge import blocks in all the numerous files, eating up precious llm context
        - all types used in a file are always imported (explicitly or via kleene)
            - this goes for typehints too
        - the inverse. no dangling imports allowed
            - but what abt * imports?
    - something about python v 3.14 vs older versions - a clue!
    - i added instruction to prune in the do-mode.md file, so it gets done bit by bit at least

3. Mull 'ExceptionPolicy'
    (this too nice but not mission critical)
    - its purpose is essentially to classify ex that core does not deal with
    - and to format the msgs that get printed in main
    - 'protection' must be done up in main
    - while formatting via method is in appengine (it receives the ex)
        - perhaps it all goes to main? 

B. Ingestion, Validation & Complaints

4. we have a Repocontract class in constants. The system should align to this.
    (yes)
    - the initialize_workspace method uses the repo contract over old system
    - on boot validation that pud fulfills contract, else complain
    
7. Landingzone for Validation / complaint ideas
    - manifest resolver must check if fileset is declared in pud_cfg with fallback to shared_cfg
        - so shared_cfg can declare defaults for it, which the pud_cfg can override
        - if not present, then complain
    - MD asset collision, fallback and absense
        - Files of Filelist, subject to the md resolver, referenced by name in Filelists, must follow these rules
            - there can be no pud internal or shared internal naming collisions
                - <pud>/onepath/file.txt and <pud>/another/file.txt is a violation
                    - because name is the same, it is pud internal collision. the same rule applies to shared internals
                - <pud>/onepath/file.txt and <shared>/another/file.txt is OK
                    - this is fine. the system uses fallback from pud to shared
        - so we need to validate that the files mentioned
            - exist in one or both repos
            - but are unique internally in any repo they exist
        - then manifest resolver can do less work

D. Testing
10. Refine and develop E2E test system
    note: must be interpreting the app, dont the test system to gulliverize progression
    Next steps
    - when the suite has run, it should offer to delete the sandbox and report directory by prompt
    - finish out the assertclass to match my manual flow for the test repo
        - after the init / repocontract assert, assert that we are in the UI
        - then assert that buttons yield expected results
            - grep the UI ouputs for messages? then we can pick up on 'domain xx selected, prompt xx this many lines / chars'
    - after manual flow of testrepo is implemented, run at grumpy Grok to see if he approves of it
    - then implement assertclass for 'unruly' repo  
        - has members that collide with contract
    - then the pud = clanker case
        - which is just spam the buttons to see that they all work as expected
            - but frequent drift of expectation, cuz not predetermined/static like with the other repos
        - also path resolution is different?

II. Items to refine & QC:
III. Slated for implementation:
IV. Recently implemented:
V. Bugs

1. on test repo, manifest domain for 'a' only (keys 1 -> a):
'''
[FATAL] FileAccessError: File access error for presentation/image_lib/clanker_boot.png: 'utf-8' codec can't decode byte 0x89 in position 0: invalid start byte

--- Underlying Stack Trace ---
Traceback (most recent call last):
  File "/home/user/Projects/Clanker-clanker/content/ports_adapters/adapters.py", line 72, in _pud_file_as_string
    return (self.pud_path / rel_path).read_text(encoding="utf-8")
           ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^
  File "/usr/lib/python3.14/pathlib/__init__.py", line 788, in read_text
    return f.read()
           ~~~~~~^^
  File "<frozen codecs>", line 325, in decode
UnicodeDecodeError: 'utf-8' codec can't decode byte 0x89 in position 0: invalid start byte

~/Projects/Clanker-clanker main*
'''
on test repo, manifest domain for 's' only (keys 1 -> s):
works fine, but dont use a manifest resolver..


2. Potential UnboundLocalError in RuamelYamlParserAdapter
-   If self.yaml.load(raw_text) returns None (which ruamel.yaml does for empty strings or comments-only files), this returns None instead of a dict. If callers expect a dict, you may want return self.yaml.load(raw_text) or {}.