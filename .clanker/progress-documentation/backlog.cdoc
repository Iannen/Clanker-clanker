I. Ideas, complaints and non-critical bugs:
- configs should be validated to disallow non-existent filesets (now just fail silently)
- filesets: new concept, 'omits' added as a manifest of files not supplied
- Visual upgrade to UI template
- the user should not have to start & stop the app for config updates to become online
- can we do without any external deps like ruamel & pydantic, lessening the install burden?
- arrow keys interpreted as esc

II. Items to refine & QC:

1a.  Leverage ports & adapters split (analysis item)
    Offload non-business logic to adapters, leave behind clean code. imports from both 'adapters' and 'protocols' are all adapters
    - SessionService
        _get_validated_cfg_fragment
            - evaluated to be ok
        get_keyboard
            - current target
                - phase 1 acquistion is OK i think
                - phase 2, the untangle begins? lets not rush it
        _resolve_sets
            - ties into 'get_keyboard' phase 2 work. lets wait
        initialize_workspace
            - Cleaned up ex handling, i think the 'files' are distinct enough that core visibility takes priority.
            - evaluated to be ok
    - AssemblyService
        hydrate
            - now uses an adapter for the transformation
            - evaluated to be ok
        get_template
            - here we can at least rationalize to do the action inside the match clauses    
        get_repl_map
            - gna leave this one alone
        _build_manifest_block
            - i think the datamodel needs a revamp to prettify this thing
        _resolve
            MULTI_DOC
                - perhaps a datamodel revamp is a prereq to prettify this
            FULL_PATH_FILE
                - not great, not terrible
            REPO_CONTENT
                - tis alright?
            KB_INFO
                - kinda nice looking?
            REPO_MANIFEST
                - looks okay, but the helper what is sex appeal of '_build_manifest_block
                - as an aside, rather than two blocks, just have one? then each path can start with the token.
            - valuerror at end should be clanker error or stricken cuz its config validation concern (thought not impl there yet)
    - IOService   
        display
            - push clear into bridge, so its a 1 step passthrough
        to_clipboard
            - ok
        get_key
            - ok i guess?
        get_confirmation
            - this one is up for review. can the bridge do more

III. Slated for implementation:

IV. Recently implemented:

V. Critical bugs
