I. Ideas, complaints and non-critical bugs:
- configs should be validated to disallow non-existent filesets (now just fail silently)
- filesets: new concept, 'omits' added as a manifest of files not supplied
- Visual upgrade to UI template
- the user should not have to start & stop the app for config updates to become online
- can we do without any external deps like ruamel & pydantic, lessening the install burden?
- arrow keys interpreted as esc

II. Items to refine & QC:

1.  Data model revamp, unused alt-mode functionality removal. We split this up into several jobs:
    - pud cfg alignment debt:
        - rename 'sets' -> 'filesets'
        - update 'shared_domains.yaml' -> 'shared_cfg.yaml'
        - update 'kb_def.yaml' -> 'system_cfg.yaml'
    [ ] 'models.py': New set of domain model classes: 
'''
class RuntimeConfig: # fully implemented
    keyboard: Keyboard
    ui_render: Render
    base_resolvers: [Resolver]

class Keyboard: # fully implemented
    button_map: dict[str, Button] 
    selected_key: str | None
    def get_unique_buttons
    def handle_key 

class Button: # needs a revision next leg
    type: str
    key: str
    inhabitant: Domain | Prompt | None = None # this is Domain | Prompt | Render | None = None in codebase atm. there should be no Render, now that Prompt is implemented
    action: Callable | None = None
    def get_repl_map

class Domain # fully implemented
    name: str
    resolvers: [Resolver]
    prompts: [Prompt]

class Prompt:  # needs revision next leg
    name: str
    renders: [Render] # this needs to be singular, non-nullable

class Render: # implemented
    template: str = "prompt_template"
    resolvers: [Resolver]
    inherit_base: bool = True
    inherit_domain: bool = True

#below stuff, not aligned / implemented I think
class Resolver:
    anchor: str
    def get_repl_map: dict[str, str]

class MultiDocResolver(Resolver):
    files: [File]

class File:
    name: str
    full_path_from_pud: bool = False
    truncation_spec: TruncationSpec

class TruncationSpec:
    # for more advanced truncation laters, for now just tailtrunc int
    tail_lines: int | None

class RepoContentResolver(Resolver):
    fileset: FileSet

class FileManifestResolver(Resolver):
    pud_fileset: Fileset
    shared_fileset: Fileset

class KBStateResolver(Resolver):

class FileSet -> to deal with filesets of repo and manifest resolver first class in domain 
    includes: [str]
    excludes: [str]
'''

1d. Revision per # embedded comments of master bl item II.1, lets hash it out

III. Slated for implementation:

IV. Recently implemented:

1c. Introduce 'Prompt' model wrapper and align Domain/Render hierarchy
    [x] 'models.py': Define Prompt class and update Domain and Render dataclasses
        - Introduce Prompt(name: str, renders: list[Render]) dataclass
        - Update Domain model to replace renders attribute with prompts: list[Prompt]
        - Update Render model to remove name attribute and add default template = "prompt_template"
    [x] 'utilities.py': Update RuntimeConfigAssembler for Prompt hierarchy
        - Update domain parsing to construct Prompt objects containing Render instances
    [x] 'clanker.py': Update GameEngine and AssemblyService to handle Prompt models
        - Update _set_selected_num_btn to populate prompt row buttons using domain prompts
        - Update _compile_to_clipboard and AssemblyService resolving/rendering to read from Prompt.renders
    [x] 'shared_cfg.yaml': Align domain prompt configurations to nest render definitions under Prompt objects
        - Move 'name' attribute to Prompt level and nest 'renders' list/config under each prompt
    [x] 'config.yaml': Align similar to 'shared_cfg.yaml'


V. Critical bugs
