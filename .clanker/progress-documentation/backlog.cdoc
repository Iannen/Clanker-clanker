I. Ideas, complaints and non-critical bugs:
- configs should be validated to disallow non-existent filesets (now just fail silently)
- filesets: new concept, 'omits' added as a manifest of files not supplied
- Visual upgrade to UI template
- the user should not have to start & stop the app for config updates to become online
- can we do without any external deps like ruamel & pydantic, lessening the install burden?
- arrow keys interpreted as esc

II. Items to refine & QC:

1.  Data model revamp, unused alt-mode functionality removal. We split this up into several jobs:
    [ ] 'models.py': New set of domain model classes: 
'''
class RuntimeConfig: # purportedly fully aligned
    keyboard: Keyboard
    ui_render: Render
    base_resolvers: [Resolver]

class Keyboard: # purportedly fully aligned
    button_map: dict[str, Button] 
    selected_key: str | None
    def get_unique_buttons
    def handle_key 

class Button:
    type: str
    key: str
    inhabitant: Domain | Prompt | None = None
    action: Callable | None = None
    def get_repl_map

class Domain
    name: str
    resolvers: [Resolver]
    prompts: [Prompt]

class Prompt 
    name: str
    renders: [Render]

class Render -> 
    template: str = "prompt_template"
    resolvers: [Resolver]
    inherit_base: bool = True
    inherit_domain: bool = True

class Resolver:
    anchor: str
    def get_repl_map: dict[str, str]

class MultiDocResolver(Resolver):
    files: [File]

class File:
    name: str
    full_path_from_pud: bool = False
    truncation_spec: TruncationSpec

class TruncationSpec:
    # for more advanced truncation laters, for now just tailtrunc int
    tail_lines: int | None

class RepoContentResolver(Resolver):
    fileset: FileSet

class FileManifestResolver(Resolver):
    pud_fileset: Fileset
    shared_fileset: Fileset

class KBStateResolver(Resolver):

class FileSet -> to deal with filesets of repo and manifest resolver first class in domain 
    includes: [str]
    excludes: [str]
'''

III. Slated for implementation:

IV. Recently implemented:

1a. Implement RuntimeConfig root container & clean configuration boundaries (is converted to hist item)
    [x] 'models.py': Define RuntimeConfig root dataclass
        - Add RuntimeConfig container holding keyboard, ui_render, and base_resolvers
        - Remove ui_render and base_resolvers from Keyboard to focus its scope on keybindings and selection state
    [x] 'utilities.py': Update RuntimeConfigAssembler & ConfigValidator
        - Update RuntimeConfigAssembler.assemble to return RuntimeConfig containing keyboard, ui_render, and base_resolvers
        - Relocate base_resolvers extraction source from kb_def.yaml to shared_cfg.yaml
        - Migrate set dictionary lookups from 'sets' to 'filesets' in both RuntimeConfigAssembler and ConfigValidator
    [x] 'clanker.py': Wire RuntimeConfig through SessionService and GameEngine
        - Replace SessionService.get_keyboard with get_runtime_config returning RuntimeConfig
        - Update GameEngine._bootstrap to store self.runtime_config and derive self.kb from it
        - Refactor GameEngine._display_ui and _compile_to_clipboard to pass RuntimeConfig to AssemblyService.get_repl_map
    [x] Configuration Files ('system_cfg.yaml' & 'shared_cfg.yaml'): Align YAML schemas & filenames with new boundaries
        - Renamed kb_def.yaml -> system_cfg.yaml and shared_domains.yaml -> shared_cfg.yaml
        - Evict resolvers block from system_cfg.yaml (retaining only UI render frame and rows)
        - Add root base_resolvers to shared_cfg.yaml for global prompt-layer resolvers
        - Rename root sets mapping to filesets in shared_cfg.yaml
        
V. Critical bugs
