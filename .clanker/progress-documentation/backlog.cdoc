I. Ideas, complaints and non-critical bugs:
- arrow keys interpreted as esc
    - this has grown on me though - keep? but formalize if so
- the rendered UI does not properly display empty buttons for 'qwerty' and 'asdfgh' rows
    - this is due to the ui template being constrained to 4 buttons for the two keyrows.
    - ez fix is to adjust template.
    - rabbithole solution is to have app write template to rowspec

II. Items to refine & QC:

0. Config & Asset validation
    - yaml configs
        - must parse properly & unambiguously into a combined RTC, in a transparent manner in the system which does it
            - yielding complaints to a collector for any violation encountered
            - then attempting to bypass it, keeping process going, so as many complaints can be collected in the pass
    - layout assets
        - must be able to contain that which the app wants to assign to them, else cause of complaint
    - content bearing assets
        - must exist, else cause of complaint 

1. UI / Feedback imp [DX/UX]: 
    - make prompt compilation msg say which domain, prompt was used in addition to just line/char
    - turns into a pipeline refactor. oh well
    - we have the righthand side of the screen to put info on. will be baller, only green (black) grass

2. i should be able to add more than just filesets as named bottom level sets.
    - MD resolver prime target, I can put it in shared cfg -> reuse it in domains of pud cfgs
    - lets not rush it tho

3. revise new dir structure, so we dont have simple 'code.py' files, they should ref their system

III. Slated for implementation:

IV. Recently implemented:

4. Improve config_assembly_system complaint production
    [x] 'content/config_assembly_system/code.py': Model DomainOverflow and record overflow during button mapping
        - add 'class DomainOverflow' containing sys_cfg_name, row_keys, domain_names, cfg_filename, and overflow_count
        - add get_msg() -> str to DomainOverflow producing formatted complaint string
        - update RuntimeConfigAssembler._create_btn_map to instantiate DomainOverflow complaints and register them with ErrorCollector when domains exceed button slot capacities
        - safely trim/omit overflowing domains during key assignment
    [x] 'content/config_assembly_system/translation_system/config_dtos.py': Integrate DomainOverflow handling into ErrorCollector
        - add internal storage for DomainOverflow complaint instances in ErrorCollector
        - add method to record DomainOverflow instances
        - implement get_domain_overflow_report() -> str | None returning concatenated report or None if empty
    [x] 'content/contracts/assembly_system_contract.py': Update Report abstract interface
        - add @abstractmethod get_domain_overflow_report(self) -> str | None to Report class

    [x] 'clanker.py': Refine complaint consumption for capacity and missing fileset cases  
        - SessionService.get_runtime_config:
            - return report & rtc to appengine
        - AppEngine.bootstrap: determines if we can proceed based on the report
            - dof_report = report.get_domain_overflow_report
            - if dof is not None
            - ask user acceptance to bootstrap, offer to exit
    conclusion: this was successful, but I must transition into a worker pipeline to maintain sanity

V. Critical bugs
