I. Ideas, complaints and non-critical bugs:
- configs should be validated to disallow non-existent filesets (now just fail silently)
- filesets: new concept, 'omits' added as a manifest of files not supplied
- Visual upgrade to UI template
- the user should not have to start & stop the app for config updates to become online
- can we do without any external deps like ruamel & pydantic, lessening the install burden?
- arrow keys interpreted as esc

II. Items to refine & QC:
   
1. Usability push
    - 1a
    - 1b
    - 1c
    - 1d


1a. expand truncation functionality
    - in addition to tail lines
        - so i can add tail of history doc
    - 'from & including line by regex match -> to but not including regex line match'
        - so I can target part of backlog

1b. move shared domains to ASDF row

1c. investigate ways to capitalize on semantics of yaml for prompt gen.
    - name attrs: domain.name, prompt.name, fileset.name, filelist.name
    - anchor names: base_fragments, the rest
    - basically, the name of a domain, name of a prompt, should feature in the prompt to provide ctx.
    - the prompt compilation system seems kinda wack now, but perhaps its ok? i dont know.
        - perhaps I add nested state? depart from inhabitant model into state = [domain][prompt] or something like that?

1d. new resolver: follow up:
    - lets the llm specify required context on some format, like an md resolver
    - accepts the llm artifact, returns requested content compiled to clipboard for dropoff
    - the file dependency issue: repo_content resolver should have some kinda of 'display omits' functionality:
        - so that files not shared can be informed of, on format like:
'''
<PUD>src/adapters/myadapter.py: xx lines
<PUD>src/adapters/myotheradapter.py: xx lines
'''

2. rationalize imports with kleene operator & more:
    [x] Relocate 'clanker.py' members to 'models.py' to align with P&A architecture:
        - 'BasePathTokens', 'IOControl', CfgFragments, DocPaths, Layout, ActionResult
        - protocol members will be moved later

    [x] 'clanker.py', 'utilities.py' and 'adapters.py': 
        - collapse huge import blocks to 'from models import *'

    - Revise who imports what. 
         - clanker.py
            -> should import all members of below three files. its basically the app.
            -> its compliant
         - models.py 
            -> should import none of the .py files of the projects
            -> its compliant 
            -> if this should hold ex declarations, then baseex functionality must be decouple from inheritance hierarchy.
         - utilities.py 
            -> should import the models only
            -> violates to gain access to clanker.py ex
         - ideally adapters.py 
            -> should ideally import the models only
            -> but its members inherit 'Bridge' for functionality that ties in with the baseex system. 
            -> so keep as-is for now. perhaps a better solution can be arrived at? the Bridge base functionality should be visible from core

III. Slated for implementation:

2c  Unify on 'Protocol' for adapter ports and utility members
    [ ] 'models.py': Centralize all port and utility protocol definitions
        - Relocate 'FileBridgePort' and 'IOBridgePort' from 'clanker.py' to 'models.py', converting them from ABC to Protocol
        - Move utility protocols ('ContentShaperProtocol', 'ConfigValidatorProtocol', 'RuntimeConfigAssemblerProtocol') to 'models.py'
        - Retain zero internal project module imports in 'models.py'
    [ ] 'adapters.py': Remove dependencies on 'clanker.py'
        - Remove 'xxxPort' inheritance from 'FileBridge' and 'IOBridge' to enable structural subtyping
        - Remove now-redundant clanker imports
    [ ] 'clanker.py': Clean up local port definitions and align type hints
        - Update service type annotations and constructor parameters to reference protocols imported from 'models.py'

    analysis for context:
    - problem: adapters.py imports from clanker.py so that the concrete adapters can inherit their ABC based 'interface'.
    - goal: resolve adapters.py dependency on clanker.py by eliminating ABC in favour of Protocol
        - achieve a 'dev-time golang' interface situation
    - unify imports and DI wiring of utilities & adapters members to python idioms. demonstration their structure:

''' utilities.py, only depends on models.py and app external dependencies
from __future__ import annotations
import copy
import re
from typing import Any
from ruamel.yaml import YAML
from models import *

class DefaultContentShaper:
#methods truncated
class ConfigValidator:
#methods truncated
class RuntimeConfigAssembler:
#methods truncated
'''

''' adapters.py. depends upon clanker.py fo the ports. 
from __future__ import annotations

import base64
import os
import sys
import termios
import tty
from pathlib import Path
from ruamel.yaml import YAML
from models import *
from clanker import FileBridgePort, IOBridgePort

class IOBridge(IOBridgePort): 
#truncated methods
class FileBridge(FileBridgePort):
#truncated methods
'''

IV. Recently implemented:

2b. Eliminate Bridge base class and decouple ports from exception policy
    constraint: does not target imports from 'utilities.py' or their protocols.
    [x] 'clanker.py': 
        Implement 'ExceptionPolicy.protect_adapter' method
            - Iterate over public callable attributes on an adapter instance using dir(), filtering out names starting with '_'
            - Wrap bound methods in-place with 'ExceptionPolicy.wrap_bridge_call' inside a single method
        - Remove 'Bridge' base class
            - Update 'IOBridgePort' and 'FileBridgePort' to inherit directly from ABC instead of Bridge
            - Update 'main()' to wrap adapter instances upon creation via 'ExceptionPolicy.protect_adapter()'

V. Critical bugs
